import React from 'react'

const Title = ({title}) => {
  return (
    <div className='text-2xl 2xl:text-3xl font-semibold text-gray-600 dark:text-gray-500'>
        {title}</div>
  )
}

export default Title;